import re

# search
# so = re.search(r'(\w+)', 'Welcome to python!')
# print so.group()
# so = re.search(r'[pP]ython', 'Match python!')
# print so.group()
#
# so = re.search(r'[pP]ython', 'Interesting python!')
# print so.group()

#
# # findall
# words = re.findall(r'\S+\s','Welcome to python!!! ')
# for word in words: print word

# numbers = re.findall(r'\d{1,3}', 'All the numbers are 11, 23, 124, 143')
# for num in numbers:
#     print num
